name: hexo build

on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      run: git pull

    - name: Build and Deploy
      run: npm run build