name: hexo build
on:
  push:
    branches:

      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:

    - name: show node version
      run: node -v

    - name: cd path
      run: npm init && npm install

    - name: build
      run: npm run build

   
